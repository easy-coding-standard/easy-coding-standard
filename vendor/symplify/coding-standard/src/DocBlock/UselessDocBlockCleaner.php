<?php

namespace Symplify\CodingStandard\DocBlock;

use ECSPrefix20210516\Nette\Utils\Strings;
use PhpCsFixer\Tokenizer\Token;
final class UselessDocBlockCleaner
{
    /**
     * @var string[]
     */
    const CLEANING_REGEXES = [self::TODO_COMMENT_BY_PHPSTORM_REGEX, self::TODO_IMPLEMENT_METHOD_COMMENT_BY_PHPSTORM_REGEX, self::COMMENT_CLASS_REGEX, self::COMMENT_CONSTRUCTOR_CLASS_REGEX, self::COMMENT_METHOD_CLASS_REGEX];
    /**
     * @see https://regex101.com/r/5fQJkz/2
     * @var string
     */
    const TODO_IMPLEMENT_METHOD_COMMENT_BY_PHPSTORM_REGEX = '#\\/\\/ TODO: Implement .*\\(\\) method.$#';
    /**
     * @see https://regex101.com/r/zayQpv/1
     * @var string
     */
    const TODO_COMMENT_BY_PHPSTORM_REGEX = '#\\/\\/ TODO: Change the autogenerated stub$#';
    /**
     * @see https://regex101.com/r/QeAiRV/1
     * @var string
     */
    const SPACE_STAR_SLASH_REGEX = '#[\\s\\*\\/]#';
    /**
     * @see https://regex101.com/r/S1wAAh/2
     * @var string
     */
    const COMMENT_METHOD_CLASS_REGEX = '#^\\s{0,}(\\/\\*{2}\\s+?)?(\\*|\\/\\/)\\s+([Gg]et|[Ss]et)\\s+[^\\s]*\\.?(\\s+\\*\\/)?$#';
    /**
     * @see https://regex101.com/r/eBux3I/1
     * @var string
     */
    const COMMENT_ANY_METHOD_CLASS_REGEX = '#^\\s{0,}(\\/\\*{2}\\s+?)?(\\*|\\/\\/)\\s+(?<obvious_method_comment>([Gg]et|[Ss]et)\\s+(.*))(\\s+\\*\\/)?$#';
    /**
     * @see https://regex101.com/r/RzTdFH/4
     * @var string
     */
    const COMMENT_CLASS_REGEX = '#(\\/\\*{2}\\s+?)?(\\*|\\/\\/)\\s+[cC]lass\\s+[^\\s]*(\\s+\\*\\/)?$#';
    /**
     * @see https://regex101.com/r/bzbxXz/2
     * @var string
     */
    const COMMENT_CONSTRUCTOR_CLASS_REGEX = '#^\\s{0,}(\\/\\*{2}\\s+?)?(\\*|\\/\\/)\\s+[^\\s]*\\s+[Cc]onstructor\\.?(\\s+\\*\\/)?$#';
    /**
     * @param int $position
     * @param string $docContent
     * @return string
     */
    public function clearDocTokenContent(array $tokens, $position, $docContent)
    {
        $position = (int) $position;
        $docContent = (string) $docContent;
        foreach (self::CLEANING_REGEXES as $cleaningRegex) {
            $docContent = \ECSPrefix20210516\Nette\Utils\Strings::replace($docContent, $cleaningRegex, '');
        }
        return $this->cleanClassMethodCommentMimicMethodName($tokens, $position, $docContent);
    }
    /**
     * @param Token[] $reversedTokens
     * @param int $index
     * @param string $docContent
     * @return string
     */
    private function cleanClassMethodCommentMimicMethodName(array $reversedTokens, $index, $docContent)
    {
        $index = (int) $index;
        $docContent = (string) $docContent;
        $matchMethodClass = \ECSPrefix20210516\Nette\Utils\Strings::match($docContent, self::COMMENT_METHOD_CLASS_REGEX);
        if ($matchMethodClass) {
            return $docContent;
        }
        if (!$this->isNextFunction($reversedTokens, $index)) {
            return $docContent;
        }
        $matchAnyMethodClass = \ECSPrefix20210516\Nette\Utils\Strings::match($docContent, self::COMMENT_ANY_METHOD_CLASS_REGEX);
        if (!$matchAnyMethodClass) {
            return $docContent;
        }
        $obviousMethodComment = $matchAnyMethodClass['obvious_method_comment'];
        $obviousMethodComment = $this->removeSpaces($obviousMethodComment);
        $methodNameContent = $reversedTokens[$index + 6]->getContent();
        if (\strtolower($obviousMethodComment) !== \strtolower($methodNameContent)) {
            return $docContent;
        }
        return \ECSPrefix20210516\Nette\Utils\Strings::replace($docContent, self::COMMENT_ANY_METHOD_CLASS_REGEX, '');
    }
    /**
     * @param int $index
     * @return bool
     */
    private function isNextFunction(array $reversedTokens, $index)
    {
        $index = (int) $index;
        if (!isset($reversedTokens[$index + 4])) {
            return \false;
        }
        return $reversedTokens[$index + 4]->getContent() === 'function';
    }
    /**
     * @param string $content
     * @return string
     */
    private function removeSpaces($content)
    {
        $content = (string) $content;
        return \ECSPrefix20210516\Nette\Utils\Strings::replace($content, self::SPACE_STAR_SLASH_REGEX, '');
    }
}
